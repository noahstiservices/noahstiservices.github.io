person = {
  name: "Noah",
  age: 27,
  country: "United States"
}

function logData() {
  myPerson = person.name + " is " + person.age + " and lives in " + person.country
  console.log(myPerson)
}

logData()
